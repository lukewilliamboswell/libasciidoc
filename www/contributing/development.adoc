= Development Setup
:weight: 1

// Goal: Get a contributor from zero to running tests in under 10 minutes.
// Assume they know Go basics but nothing about this project. Cover:
// - Prerequisites (Go version, any other tools)
// - Clone and build
// - Run the test suite
// - Run the site locally for docs changes
// - Common development tasks (regenerating the parser, etc.)

== Prerequisites

* https://go.dev/dl/[Go] 1.21 or later
* Git

== Clone and Build

 git clone https://github.com/lukewilliamboswell/libasciidoc.git
 cd libasciidoc
 go build ./...

Build the CLI tools:

 go build -o ascii2html ./cmd/ascii2html
 go build -o ascii2doc ./cmd/ascii2doc

== Run Tests

 go test ./...

// TODO: Document any test flags or patterns that are useful during development.
// For example, how to run tests for a specific package, how to update golden
// files if we use them, how to run only short tests, etc.

== Run the Site Locally

To preview documentation changes:

 ./serve.sh

This builds `ascii2doc`, generates the site into `_site/`, and serves it on `http://localhost:8080`.

== Project Structure

// TODO: Add a brief directory listing showing where the main packages live.
// This helps new contributors orient themselves before reading the full
// architecture guide. Something like:
//
// cmd/           — CLI entry points (ascii2html, ascii2doc)
// asciidoc/      — public library API
// configuration/ — configuration types and options
// types/         — shared type definitions
// internal/
//   parser/      — PEG-based AsciiDoc parser
//   renderer/    — output renderers (HTML5, XHTML5, DOCX)
//   cli/         — shared CLI infrastructure
//   site/        — static site generator
// www/           — website source (AsciiDoc files)

See the link:architecture.html[Architecture Overview] for a detailed explanation of how the packages fit together.

== Modifying the Grammar

The parser is generated from a PEG grammar using https://github.com/mna/pigeon[pigeon]. If you need to modify the grammar:

 go generate ./...

Then build as normal:

 go build ./...

== Benchmarking

This project uses https://onsi.github.io/ginkgo/[Ginkgo] for testing but the standard Go toolchain for benchmarking. Skip normal tests when benchmarking with `-run=XXX`:

 go test -run=XXX -bench=. -benchmem -count=10 ./... | tee tmp/bench-before.txt

After making changes, run the same benchmarks and compare with https://pkg.go.dev/golang.org/x/perf/cmd/benchstat[benchstat]:

 go test -run=XXX -bench=. -benchmem -count=10 ./... | tee tmp/bench-after.txt
 benchstat tmp/bench-before.txt tmp/bench-after.txt

Benchmark output doesn't need to be committed, but including relevant benchstat results in commit messages is helpful.
