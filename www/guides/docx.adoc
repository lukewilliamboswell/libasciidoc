= DOCX Conversion
:weight: 2

// Goal: This guide serves users who need to produce professional Word documents.
// Many of these users may be in corporate or government environments where DOCX
// is required. They care about:
// - Consistent, professional styling (fonts, colors, spacing)
// - Headers, footers, page numbering
// - Table of contents generation
// - Being able to hand the output to non-technical colleagues
//
// The theme system reuses Asciidoctor PDF's YAML theme format, which is a big
// selling point — users familiar with Asciidoctor can bring their existing themes.

== Overview

The `ascii2doc` command converts AsciiDoc files to DOCX (Microsoft Word) format. You can control the styling of the output using theme files.

== Basic Conversion

 ascii2doc myfile.adoc

This produces `myfile.docx` using default styling.

== Using Themes

Themes let you control fonts, colors, spacing, headers, footers, and more. libasciidoc uses the same YAML theme format as https://docs.asciidoctor.org/pdf-converter/latest/theme/[Asciidoctor PDF themes], so you can reuse existing themes.

 ascii2doc --theme my-theme.yml myfile.adoc

// TODO: Add a section showing a minimal example theme YAML file so users can
// see the structure without reading the full Asciidoctor PDF theme docs.
// Include at least: font family, heading colors, page margins.

== What Themes Control

// TODO: Document which theme properties are currently supported in our DOCX
// renderer. Not all Asciidoctor PDF theme properties may be implemented yet.
// Be honest about what works and what doesn't — users will trust us more
// if we're upfront about limitations.

Themes can control:

* Font families and sizes for body text, headings, and code blocks
* Colors for text, headings, and links
* Page margins and spacing
* Headers and footers with page numbers
* Table of contents depth and formatting
* Title page styling

== Tips

* DOCX files can be opened in Microsoft Word, LibreOffice Writer, or Google Docs
* For further customization beyond what themes offer, you can open the generated DOCX and modify styles directly in your word processor
* Tables are rendered full-width by default for a clean, professional look
