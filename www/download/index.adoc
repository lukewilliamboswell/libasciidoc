= Download
:weight: 2

// Goal: This page serves the non-technical user persona primarily. They want to
// get the tools onto their machine with minimum friction. We should offer:
// 1. Pre-built binaries (once we set up GitHub Releases) — the primary path
// 2. Install via `go install` — for users who already have Go
// 3. Build from source — for users who want the latest or a custom build
//
// For each option, keep the instructions copy-pasteable and platform-aware
// (macOS, Linux, Windows). Avoid assuming the user knows what PATH means.

== Pre-built Binaries

// TODO: Once we create a GitHub Release with pre-built binaries, add download
// links here for each platform (macOS arm64, macOS amd64, Linux amd64, Windows).
// Use a table or a clear list so users can find their platform quickly.
// Consider linking directly to the latest GitHub Release page.

Pre-built binaries are coming soon. In the meantime, you can install using one of the methods below.

== Install with Go

If you have https://go.dev/dl/[Go] installed (version 1.21 or later), you can install directly:

 go install github.com/lukewilliamboswell/libasciidoc/cmd/ascii2doc@latest
 go install github.com/lukewilliamboswell/libasciidoc/cmd/ascii2html@latest

This installs two commands:

* `ascii2html` — converts AsciiDoc to HTML5 (default) or XHTML5
* `ascii2doc` — converts AsciiDoc to DOCX (default), with HTML output also available

== Build from Source

Clone the repository and build:

 git clone https://github.com/lukewilliamboswell/libasciidoc.git
 cd libasciidoc
 go build -o ascii2html ./cmd/ascii2html
 go build -o ascii2doc ./cmd/ascii2doc

== Verify Installation

After installing, verify the tools are working:

 ascii2html version
 ascii2doc version

== Next Steps

Head to the link:../guides/getting-started.html[Getting Started] guide to learn how to convert your first document.
